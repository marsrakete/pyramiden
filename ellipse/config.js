export const coneDimensions = Object.freeze({ height: 5, radius: 3 });
export const defaults = Object.freeze({
  angle: 38,
  height: 2.8,
  opacity: 0.25,
});
export const maximumControlHeight = 3.8;
export const sampleCount = 360;
